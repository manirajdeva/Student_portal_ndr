# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Build
dist/
build/

# Logs
logs/
*.log

# Credentials
credentials.json
service-account.json

# clasp (Apps Script CLI) — project-specific, not shared across machines
.clasp.json
.clasprc.json

# Android TWA project — contains a signing keystore + password that must
# never be committed (a public repo leak would let anyone impersonate the
# app's identity). Back these up outside git instead.
android-twa/
